# Pure-Python deps only — installs cleanly on Python 3.14 (no wheels to compile).
Django>=5.2
djangorestframework>=3.15
drf-spectacular>=0.28
django-cors-headers>=4.4
Faker>=30.0
Pillow>=10.0
icecream>=2.1
python-dotenv>=1.0
